# 验收标准 (Acceptance Criteria)

本文档记录用户侧确认的通用验收口径。具体功能的详细验收用例可以继续写入 `TEST_PLAN.md` 或 `specs/<feature>/acceptance.md`。

## 1. 通用完成定义

一个需求默认满足以下条件才视为完成：
- 用户目标已实现，核心流程可手动走通。
- 相关边界条件已处理或明确记录为限制。
- 没有引入明显回归。
- 涉及 API、数据结构、架构、元数据表时，相关文档已同步。
- 已按项目规则完成 `git commit`。

## 2. 文档验收

- 新需求必须能追溯到 `PRD.md` 或对应 feature spec。
- 涉及业务规则变化时，必须更新 `BUSINESS_RULES.md`。
- 涉及用户验收口径变化时，必须更新本文档或 feature acceptance。
- 涉及元数据表结构变化时，必须更新 `metadata_schema.md`。
- 涉及架构、职责或部署变化时，必须更新 `ARCHITECTURE.md` 或 `4_plus_1_views.md`。
- 涉及启动、依赖、运行方式变化时，必须更新 `README.md` 或 `front-end/README.md`。

## 3. 数据源验收

- 新建有效数据源应保存成功，并能在工作台读取表列表。
- 无效数据源必须返回明确错误，不得保存脏配置。
- 编辑数据源时，密码留空不得覆盖原密码。
- 删除数据源时，不得导致前端页面崩溃；引用该数据源的查询处理方式需在需求规格中明确。

## 4. 查询验收

- 原生 SQL 成功执行后，表头、数据行和总数语义正确。
- 分页参数应正确传到后端并影响结果。
- 宏变量合法值应正确替换。
- 宏变量非法值应被后端拒绝执行。
- 查询缓存不得跨数据源、SQL 或分页参数误命中。

## 5. 看板验收

- Dashboard 可以创建、重命名、删除。
- Widget 可以添加、移除、拖拽、缩放并保存布局。
- 退出编辑模式后刷新页面，布局应保持。
- 表格 Widget 分页应可用。
- 大结果集不应造成明显拖拽卡顿；如存在限制，应在规格中写清数据量边界。

## 6. 明细穿透验收

- 点击指标应打开明细弹窗。
- 后端应保留原 SQL 的 `FROM`、`JOIN`、`WHERE` 上下文。
- 点击行的维度值应作为额外过滤条件生效。
- `COUNT(DISTINCT)` 应尽量返回去重后的实体明细。
- 明细分页切换时，应保持原 SQL、宏变量、过滤上下文和点击指标。

## 7. 导出验收

- 工作台、看板 Widget 和明细弹窗的导出入口应可用。
- 导出的 Excel 应包含当前目标数据的表头和数据。
- 配置阈值的表格导出时，应尽量保留背景色和字体可读性。

## 8. 数据对比验收

- 对比配置可以保存、更新、删除，刷新页面后仍可复用。
- 同一 SQL 使用 baseline/target 宏变量时，两侧宏应分别生效。
- 完全一致数据应只产生一致数，不应产生不一致或缺失。
- 字段值不同应计入不一致，明细应展示 baseline 值、target 值、差值、变化率和差异字段。
- baseline-only key 应计入 target 缺失，target-only key 应计入 baseline 缺失。
- 配置分组列后，每个分组独立统计数量和一致率。
- 重复 key、非法宏变量、超过最大行数都必须返回清晰错误。
- 汇总表和明细弹窗导出的 Excel 应包含正确列和值。

## 9. 后续验收追加区

```text
### AC-YYYYMMDD-001 验收项标题
- 关联需求：
- 前置条件：
- 操作步骤：
- 预期结果：
- 不通过时的处理：
```
