# 产品需求文档 (PRD)

本文档记录用户侧的产品目标、用户角色、核心流程和后续需求池。后续新增需求优先补充到本文档，再进入具体 `specs/<feature>/` 规格拆解。

## 1. 产品定位

数据可视化报表平台面向需要自助查数、固化查询组件、搭建数据看板并进行指标明细穿透的分析和业务团队。

平台当前定位为企业内部 BI 原型系统：
- 支持多数据源接入与表结构探索。
- 支持原生 SQL 查询、图表预览和查询组件固化。
- 支持拖拽式数据看板。
- 支持表格和图表指标的 Drill-through 明细穿透。
- 支持宏变量驱动的动态表版本或动态筛选。

## 2. 用户角色

| 角色 | 主要诉求 | 当前系统入口 |
| :--- | :--- | :--- |
| 系统管理员 / DevOps | 配置和验证业务数据源连接 | 配置中心 |
| 数据分析师 | 编写 SQL、调试结果、保存查询组件 | 分析工作台 |
| 业务用户 / 管理层 | 查看看板、点击指标追溯明细 | 数据看板 |

## 3. 核心用户流程

### 3.1 数据源接入
1. 管理员在配置中心新增 ClickHouse 或 DuckDB 数据源。
2. 系统执行连接测试。
3. 数据源保存到 PostgreSQL 元数据库。
4. 分析工作台可切换该数据源并读取物理表列表。

### 3.2 查询组件生产
1. 分析师选择数据源。
2. 编写或从物理表快速生成 SQL。
3. 执行查询并查看表格或图表预览。
4. 配置宏变量、图表类型和阈值规则。
5. 保存为可复用 Query 组件。

### 3.3 看板搭建
1. 用户创建 Dashboard。
2. 将 Saved Query 添加为 Widget。
3. 进入编辑模式拖拽、缩放和排列组件。
4. 保存布局。
5. 业务用户浏览看板。

### 3.4 明细穿透
1. 用户点击表格指标单元格或 ECharts 图形元素。
2. 前端传递原 SQL、维度过滤上下文、点击指标、宏变量和分页参数。
3. 后端基于 `sqlglot` 解析并重写 SQL。
4. 系统返回底层明细数据和总数。

## 4. 当前功能范围

| 模块 | 当前能力 |
| :--- | :--- |
| 数据源 | ClickHouse、DuckDB 连接配置和连通性测试 |
| 查询 | 原生 SQL 查询、分页、宏变量预编译、查询缓存 |
| 图表 | Table、Bar、Line、Pie |
| 看板 | Dashboard CRUD、Widget 拖拽布局、表格 Widget 分页 |
| 下钻 | Drill-down 聚合下钻、Drill-through 明细穿透 |
| 导出 | Excel 导出，支持阈值样式保留 |
| 元数据 | PostgreSQL 保存数据源、查询组件、看板、布局 |

## 5. 非目标

当前阶段暂不作为默认目标：
- 多租户、组织、用户权限体系。
- 完整发布级数据权限隔离。
- 自动化调度和报表订阅。
- 大规模生产集群部署方案。
- 可视化低代码建模器。

## 6. 后续需求池

后续新增需求按以下格式追加：

```text
### REQ-YYYYMMDD-001 需求标题
- 提出日期：
- 提出人：
- 用户角色：
- 背景：
- 目标：
- 非目标：
- 优先级：P0 / P1 / P2
- 验收摘要：
- 关联规格目录：specs/<feature>/
```

